name: Release Request

on:
  pull_request:
    types:
      - closed
      - edited
      - labeled
      - opened
      - unlabeled
    branches:
      - master

jobs:
  process:
    permissions:
      contents: write
    uses: ./.github/workflows/release-request.yml
