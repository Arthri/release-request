name: Release Request

on:
  pull_request:
    types:
      - closed
      - edited
      - labeled
      - opened
      - unlabeled
    branches:
      - master

jobs:
  handle:
    permissions:
      contents: write
      pull-requests: write
    uses: ./.github/workflows/i.yml
